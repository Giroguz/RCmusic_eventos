const supabaseUrl = process.env.SUPABASE_URL || 'https://fzqpmpgbubpmongodcat.supabase.co'; const supabaseKey = process.env.SUPABASE_ANON_KEY || ''; async function rpc(name, body) { const response = await fetch(`${supabaseUrl}/rest/v1/rpc/${name}`, { method: 'POST', headers: { apikey: supabaseKey, Authorization: `Bearer ${supabaseKey}`, 'Content-Type': 'application/json' }, body: JSON.stringify(body) }); const data = await response.json().catch(() => null); if (!response.ok) throw new Error(data?.message || data?.error_description || 'Supabase request failed'); return data; } export function installDjProxy(app) { app.post('/api/dj/login', async (req, res) => { try { res.json(await rpc('dj_login', { p_email: String(req.body?.email || '').trim().toLowerCase(), p_code: String(req.body?.code || '').trim() })); } catch (error) { res.status(400).json({ error: error.message }); } }); app.post('/api/dj/events', async (req, res) => { try { res.json(await rpc('dj_get_events', { p_token: String(req.body?.token || '') })); } catch (error) { res.status(400).json({ error: error.message }); } }); app.post('/api/dj/access', async (req, res) => { try { res.json(await rpc('dj_check_access', { p_token: String(req.body?.token || '') })); } catch (error) { res.status(400).json({ error: error.message }); } }); }
