import { useEffect, useState } from 'react'
import { Check, Lock, Plus, RefreshCw, ShieldCheck, UserCheck, UserX, X } from 'lucide-react'
import { adminCreateDj, adminListDjs, adminRegenerateCode, adminSetDjPlan, adminSetDjState } from '../lib/supabase'
import { useLanguage } from '../lib/i18n'
import LanguagePicker from './LanguagePicker'

export default function AdminPanel({ session, onClose }) {
  const { t } = useLanguage(); const [djs, setDjs] = useState([]); const [form, setForm] = useState({ email: '', displayName: '', planType: 'monthly' }); const [notice, setNotice] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false)
  async function load() { try { setDjs(await adminListDjs(session.token)); setError('') } catch { setError(t('adminLoadError')) } }
  useEffect(() => { load() }, [])
  async function create(e) { e.preventDefault(); setBusy(true); setError(''); try { const result = await adminCreateDj(form, session.token); setNotice(`${t('generatedCode')}: ${result.generatedCode} · ${result.email}`); setForm({ email: '', displayName: '', planType: 'monthly' }); await load() } catch { setError(t('createDjError')) } finally { setBusy(false) } }
  async function action(fn) { setBusy(true); setError(''); try { const result = await fn(); if (result?.generatedCode) setNotice(`${t('generatedCode')}: ${result.generatedCode}`); await load() } catch { setError(t('updateDjError')) } finally { setBusy(false) } }
  return <div className="fixed inset-0 z-40 overflow-y-auto bg-black/80 p-4 backdrop-blur-sm"><div className="mx-auto my-6 max-w-5xl rounded-[2rem] border border-white/10 bg-ink p-5 sm:p-8"><div className="mb-6 flex items-start justify-between gap-4"><div><p className="eyebrow text-violet-200">{t('admin')}</p><h2 className="mt-2 font-display text-3xl font-bold">{t('djManagement')}</h2><p className="mt-2 text-sm text-white/50">{t('djManagementHint')}</p></div><div className="flex items-center gap-2"><LanguagePicker compact /><button onClick={() => action(() => adminRegenerateCode(session.dj_id, session.token))} disabled={busy} className="btn-secondary px-3 py-2 text-xs"><RefreshCw size={14} /> {t('regenerate')}</button><button onClick={onClose} className="rounded-xl p-2 text-white/50 hover:bg-white/10" aria-label={t('close')}><X size={20} /></button></div></div>{notice && <div className="mb-4 flex items-center gap-2 rounded-xl border border-neon/20 bg-neon/10 px-3 py-2 text-sm text-neon"><Check size={16} /> {notice}</div>}{error && <div className="mb-4 rounded-xl border border-red-400/20 bg-red-400/10 px-3 py-2 text-sm text-red-200">{error}</div>}<form onSubmit={create} className="mb-7 grid gap-3 rounded-2xl border border-white/10 bg-white/[.03] p-4 md:grid-cols-[1.2fr_1fr_180px_auto]"><input required type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} className="input-dark" placeholder="dj@correo.com" aria-label={t('email')} /><input required value={form.displayName} onChange={(e) => setForm({ ...form, displayName: e.target.value })} className="input-dark" placeholder={t('displayName')} aria-label={t('displayName')} /><select value={form.planType} onChange={(e) => setForm({ ...form, planType: e.target.value })} className="input-dark" aria-label={t('monthly')}><option value="monthly">{t('monthly')}</option><option value="annual">{t('annual')}</option><option value="none">{t('noPlan')}</option></select><button disabled={busy} className="btn-primary"><Plus size={17} /> {t('createDj')}</button></form><div className="space-y-3">{djs.filter((dj) => dj.role !== 'admin').map((dj) => <div key={dj.id} className="grid gap-3 rounded-2xl border border-white/10 bg-white/[.03] p-4 lg:grid-cols-[minmax(0,1fr)_180px_auto]"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><p className="font-bold">{dj.displayName}</p><span className="text-xs text-white/45">{dj.email}</span><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${dj.isActive ? 'bg-emerald-400/15 text-emerald-200' : 'bg-white/10 text-white/45'}`}>{dj.isActive ? t('active') : dj.planExpiresAt ? t('expired') : t('noPlan')}</span></div><p className="mt-2 text-xs text-white/45">{dj.daysUsed} {t('daysUsed')} · {dj.daysRemaining} {t('daysRemaining')} · {dj.planType === 'annual' ? t('annual') : dj.planType === 'monthly' ? t('monthly') : t('noPlan')}</p></div><select value={dj.planType} onChange={(e) => action(() => adminSetDjPlan(dj.id, e.target.value, session.token))} disabled={busy} className="input-dark py-2 text-xs" aria-label={t('activate')}><option value="none">{t('noPlan')}</option><option value="monthly">{t('monthly')}</option><option value="annual">{t('annual')}</option></select><div className="flex flex-wrap gap-2"><button onClick={() => action(() => adminSetDjState(dj.id, dj.blocked ? { approved: true, blocked: false } : dj.approved ? { approved: false, blocked: true } : { approved: true, blocked: false }, session.token))} disabled={busy} className="btn-secondary px-3 py-2 text-xs">{dj.blocked ? <UserCheck size={14} /> : dj.approved ? <UserX size={14} /> : <UserCheck size={14} />}{dj.blocked ? t('unblock') : dj.approved ? t('block') : t('approve')}</button><button onClick={() => action(() => adminRegenerateCode(dj.id, session.token))} disabled={busy} className="btn-secondary px-3 py-2 text-xs" title={t('regenerate')}><RefreshCw size={14} /> <span className="hidden sm:inline">{t('regenerate')}</span></button></div></div>)}{!djs.filter((dj) => dj.role !== 'admin').length && <div className="rounded-2xl border border-dashed border-white/10 px-5 py-10 text-center text-sm text-white/40"><ShieldCheck size={22} className="mx-auto mb-2 text-violet-200" />{t('noDjs')}</div>}</div><p className="mt-6 flex items-center gap-2 text-xs text-white/35"><Lock size={13} /> {t('codeOnce')} {t('noOnlinePayments')}</p></div></div>
}
