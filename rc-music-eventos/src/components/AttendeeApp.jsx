import { useEffect, useMemo, useState } from 'react'
import { Check, Loader2, Music2, Play, Search, Send, ThumbsUp, X } from 'lucide-react'
import { PageContainer, AppShell } from './Brand'
import { getLikedIds, saveLikedIds } from '../lib/storage'
import { searchTracks } from '../lib/music'
import { addSongRequest, getPublicEvent, likeSongRequest, supabaseEnabled } from '../lib/supabase'
import { useLanguage } from '../lib/i18n'

function Modal({ title, onClose, children }) { return <div className="fixed inset-0 z-50 grid place-items-center bg-black/75 p-4" onMouseDown={onClose}><div className="glass max-h-[90vh] w-full max-w-lg overflow-y-auto rounded-[2rem] p-5" onMouseDown={(e) => e.stopPropagation()}><div className="mb-5 flex items-center justify-between"><h2 className="font-display text-xl font-bold">{title}</h2><button onClick={onClose} className="rounded-xl p-2 text-white/50" aria-label="Close"><X size={20} /></button></div>{children}</div></div> }
function Preview({ track }) { const id = String(track.id || track.videoId || '').replace(/^youtube:/, ''); if (track.previewUrl) return <audio controls autoPlay src={track.previewUrl} className="w-full" />; return <iframe title={track.title} src={track.videoUrl || `https://www.youtube-nocookie.com/embed/${id}?autoplay=1&rel=0`} className="aspect-video w-full" allow="autoplay; encrypted-media" allowFullScreen /> }
function Result({ track, onPreview, onRequest, t }) { return <div className="flex min-w-0 items-center gap-3 rounded-2xl border border-white/10 bg-white/[.04] p-3"><button onClick={() => onPreview(track)} className="grid h-14 w-14 shrink-0 place-items-center overflow-hidden rounded-xl bg-white/10"><>{track.thumbnail ? <img src={track.thumbnail} alt="" className="h-full w-full object-cover" /> : <Play size={18} />}</></button><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{track.title}</p><p className="truncate text-xs text-white/50">{track.artist}</p></div><button onClick={() => onRequest(track)} className="rounded-xl bg-neon/10 px-3 py-2 text-xs font-bold text-neon">{t('order')}</button></div> }
function Request({ request, liked, onLike, onPreview, readOnly, t }) { const status = request.status === 'played' ? t('alreadyPlayed') : request.status === 'not-found' ? t('notLocated') : t('inQueue'); const tone = request.status === 'played' ? 'border-emerald-400/40 text-emerald-300' : request.status === 'not-found' ? 'border-[#ff2b6d]/45 text-[#ff4f86]' : 'border-amber-300/40 text-amber-300'; return <article className={`flex min-w-0 items-center gap-3 rounded-2xl border bg-white/[.03] p-3 ${tone}`}><button onClick={() => onPreview(request)} className="h-14 w-16 shrink-0 overflow-hidden rounded-xl bg-white/10"><img src={request.thumbnail} alt="" className="h-full w-full object-cover" /></button><div className="min-w-0 flex-1"><p className="line-clamp-2 text-sm font-bold text-white">{request.title}</p><p className="truncate text-xs text-white/50">{request.artist}</p><p className="mt-1 text-[11px] font-bold">{t('statusLabel')}: {status}</p><p className="text-[10px] text-white/40">{t('requestedBy')}: {request.requester || '—'}</p></div><button disabled={readOnly || liked || request.status !== 'pending'} onClick={() => onLike(request.id)} className="flex shrink-0 items-center gap-1 rounded-xl bg-white/10 px-2 py-1 text-xs text-white/70"><ThumbsUp size={13} fill={liked ? 'currentColor' : 'none'} />{request.likes}</button></article> }
export default function AttendeeApp({ event, onUpdate, onExit }) {
  const { t } = useLanguage(); const [query, setQuery] = useState(''); const [results, setResults] = useState([]); const [searching, setSearching] = useState(false); const [selected, setSelected] = useState(null); const [requestSong, setRequestSong] = useState(null); const [form, setForm] = useState({ requester: '', dedication: '' }); const [notice, setNotice] = useState(''); const [likedIds, setLikedIds] = useState(() => getLikedIds(event.id))
  useEffect(() => { if (!supabaseEnabled || event.localOnly) return undefined; const timer = setInterval(async () => { try { const fresh = await getPublicEvent(event.code); if (fresh) onUpdate(fresh) } catch {} }, 10000); return () => clearInterval(timer) }, [event.code, event.localOnly, onUpdate])
  const requests = useMemo(() => [...(event.requests || [])].filter((r) => !['awaiting-payment', 'payment-rejected'].includes(r.status)).sort((a, b) => b.likes - a.likes), [event.requests])
  async function handleSearch(e) { e.preventDefault(); if (!query.trim()) return; setSearching(true); try { setResults(await searchTracks(query)) } catch { setNotice('No se pudo buscar ahora.') } finally { setSearching(false) } }
  async function submit(e) { e.preventDefault(); if (event.finalized || !requestSong || !form.requester.trim() || (event.tipsRequired && !form.paymentProof)) return; try { const request = supabaseEnabled && !event.localOnly ? await addSongRequest(event.id, requestSong, form) : { id: `request-${Date.now()}`, title: requestSong.title, artist: requestSong.artist, videoId: requestSong.id, source: requestSong.source || 'youtube', thumbnail: requestSong.thumbnail, previewUrl: requestSong.previewUrl, requester: form.requester.trim(), dedication: form.dedication.trim(), likes: 0, status: 'pending' }; onUpdate({ ...event, requests: [...(event.requests || []), request] }); setRequestSong(null); setForm({ requester: '', dedication: '' }); setResults([]); setQuery(''); setNotice(t('sent')); setTimeout(() => setNotice(''), 3500) } catch { setNotice('No se pudo enviar el pedido.') } }
  async function like(id) { if (event.finalized || likedIds.includes(id)) return; try { if (supabaseEnabled && !event.localOnly) await likeSongRequest(id); const next = [...likedIds, id]; setLikedIds(next); saveLikedIds(event.id, next); onUpdate({ ...event, requests: event.requests.map((r) => r.id === id ? { ...r, likes: r.likes + 1 } : r) }) } catch { setNotice('No se pudo registrar el voto.') } }
  return <AppShell onHome={onExit} right={<span className="rounded-xl border border-neon/20 bg-neon/10 px-3 py-2 text-sm font-bold tracking-widest text-neon">{event.code}</span>}><PageContainer><div className="mb-7"><p className="eyebrow">{t('attendee')} · {event.code}</p><h1 className="mt-2 font-display text-4xl font-bold">{t('attendeeAction')}.</h1><p className="mt-3 text-sm text-white/50">{t('requestHint')} {event.djName}.</p></div>{notice && <div className="mb-5 flex items-center gap-2 rounded-2xl border border-neon/20 bg-neon/10 px-4 py-3 text-sm text-neon"><Check size={18} />{notice}</div>}{!event.finalized && <section className="glass rounded-[2rem] p-5 sm:p-7"><div className="mb-4 flex items-center justify-between"><div><p className="eyebrow">{t('findRhythm')}</p><h2 className="section-title mt-2">{t('attendeeSearch')}</h2></div><Music2 className="text-white/20" /></div><form onSubmit={handleSearch} className="flex gap-2"><input value={query} onChange={(e) => setQuery(e.target.value)} className="input-dark flex-1" placeholder={t('searchPlaceholder')} /><button className="btn-primary px-4" disabled={searching || !query.trim()}>{searching ? <Loader2 size={18} className="animate-spin" /> : <Search size={18} />}</button></form><div className="mt-4 space-y-2">{results.map((track) => <Result key={`${track.id}-${track.title}`} track={track} t={t} onPreview={setSelected} onRequest={(track) => { setRequestSong(track); setForm({ requester: '', dedication: '' }) }} />)}</div></section>}<section className="mx-auto mt-8 max-w-3xl"><div className="mb-4 flex items-end justify-between"><div><p className="eyebrow">{t('partyQueue')}</p><h2 className="section-title mt-2">{t('liveRequests')}</h2></div><span className="rounded-full bg-white/10 px-3 py-1.5 text-xs text-white/50">{requests.length}</span></div><div className="space-y-3">{requests.map((request) => <Request key={request.id} request={request} liked={likedIds.includes(request.id)} onLike={like} onPreview={setSelected} readOnly={event.finalized} t={t} />)}</div></section></PageContainer>{requestSong && <Modal title={t('requestSong')} onClose={() => setRequestSong(null)}><form onSubmit={submit} className="space-y-4"><div className="rounded-2xl bg-white/[.05] p-3"><p className="font-bold">{requestSong.title}</p><p className="mt-1 text-xs text-white/50">{requestSong.artist}</p></div><input required value={form.requester} onChange={(e) => setForm({ ...form, requester: e.target.value })} className="input-dark w-full" placeholder={t('namePlaceholder')} /><textarea value={form.dedication} onChange={(e) => setForm({ ...form, dedication: e.target.value })} className="input-dark min-h-24 w-full resize-none" placeholder={t('dedicationPlaceholder')} /><button className="btn-primary w-full"><Send size={17} />{t('sendRequest')}</button></form></Modal>}{selected && <Modal title={t('preview')} onClose={() => setSelected(null)}><div className="overflow-hidden rounded-2xl bg-black"><Preview track={selected} /></div><h3 className="mt-4 font-bold">{selected.title}</h3><p className="mt-1 text-sm text-white/50">{selected.artist}</p></Modal>}</AppShell>
}
