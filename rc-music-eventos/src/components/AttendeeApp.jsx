import { useMemo, useState } from 'react'
import { ArrowLeft, Check, Clock3, Loader2, MessageCircleHeart, Music2, Play, Search, Send, ThumbsUp, X, Zap } from 'lucide-react'
import { PageContainer, AppShell } from './Brand'
import { getLikedIds, saveLikedIds } from '../lib/storage'
import { searchTracks } from '../lib/music'
import { addSongRequest, likeSongRequest, supabaseEnabled } from '../lib/supabase'
import { useLanguage } from '../lib/i18n'

function Modal({ children, onClose, title }) { return <div className="fixed inset-0 z-50 grid place-items-center bg-black/75 p-4 backdrop-blur-sm" onMouseDown={onClose}><div className="glass max-h-[90vh] w-full max-w-lg overflow-y-auto rounded-[2rem] p-5 shadow-2xl sm:p-7" onMouseDown={(e) => e.stopPropagation()}><div className="mb-6 flex items-center justify-between"><h2 className="font-display text-2xl font-bold">{title}</h2><button onClick={onClose} className="rounded-xl p-2 text-white/50 hover:bg-white/10 hover:text-white" aria-label="Close"><X size={20} /></button></div>{children}</div></div> }
function SearchResult({ track, onPreview, onRequest, t }) {
  const sourceLabel = { youtube: 'YouTube', deezer: 'Deezer', soundcloud: 'SoundCloud', spotify: 'Spotify' }[track.source] || track.source
  if (track.external) return <div className="flex min-w-0 items-center justify-between gap-3 rounded-2xl border border-white/10 bg-white/[.035] p-3"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><p className="break-words text-sm font-semibold">{track.title}</p><span className="rounded-md bg-white/10 px-1.5 py-0.5 text-[9px] font-bold uppercase tracking-wide text-white/45">{sourceLabel}</span></div><p className="mt-1 text-xs text-white/50">{track.artist}</p></div><a href={track.externalUrl} target="_blank" rel="noreferrer" className="shrink-0 rounded-xl bg-neon/10 px-2.5 py-2 text-xs font-bold text-neon transition hover:bg-neon hover:text-ink">{t('openProvider')}</a></div>
  return <div className="search-result group grid min-w-0 items-center gap-2 rounded-2xl border border-white/10 bg-white/[.035] p-2 transition hover:border-white/20 hover:bg-white/[.06] sm:gap-3 sm:p-2.5" style={{ gridTemplateColumns: '58px minmax(0, 1fr) auto' }}><button onClick={() => onPreview(track)} className="relative h-14 w-14 shrink-0 overflow-hidden rounded-xl bg-white/10 sm:h-16 sm:w-[76px]" aria-label={`${t('preview')}: ${track.title}`}><>{track.thumbnail ? <img src={track.thumbnail} alt="" className="h-full w-full object-cover opacity-80 transition group-hover:opacity-100" /> : <div className="grid h-full w-full place-items-center text-[10px] font-bold uppercase text-white/40">{sourceLabel}</div>}</><span className="absolute inset-0 grid place-items-center bg-black/25"><span className="grid h-6 w-6 place-items-center rounded-full bg-white text-ink"><Play size={12} fill="currentColor" /></span></span></button><div className="min-w-0 py-0.5"><div className="flex min-w-0 items-center gap-1.5"><p className="search-result__title min-w-0 text-[13px] font-semibold leading-[1.2]" style={{ display: '-webkit-box', WebkitBoxOrient: 'vertical', WebkitLineClamp: 2, overflow: 'hidden' }}>{track.title}</p><span className="shrink-0 rounded-md bg-white/10 px-1.5 py-0.5 text-[8px] font-bold uppercase tracking-wide text-white/45">{sourceLabel}</span></div><p className="mt-1 truncate text-[11px] leading-4 text-white/55">{track.artist}</p></div><button onClick={() => onRequest(track)} className="self-center rounded-xl bg-neon/10 px-2 py-1.5 text-[11px] font-bold text-neon transition hover:bg-neon hover:text-ink sm:px-3 sm:py-2 sm:text-xs">{t('order')}</button></div>
}
function RequestCard({ request, liked, onLike, onPreview, t }) { const statusLabel = request.status === 'played' ? t('alreadyPlayed') : request.status === 'not-found' ? t('notLocated') : t('inQueue'); const handled = request.status !== 'pending'; const statusTone = request.status === 'played' ? 'border-emerald-400/30 bg-emerald-400/[.06]' : request.status === 'not-found' ? 'border-[#ff2b6d]/40 bg-[#ff2b6d]/[.06]' : 'border-amber-300/30 bg-amber-300/[.05]'; const statusText = request.status === 'played' ? 'text-emerald-300' : request.status === 'not-found' ? 'text-[#ff4f86]' : 'text-amber-300'; return <article className={`live-request-card flex min-w-0 items-center gap-2 rounded-2xl border px-2.5 py-2.5 transition ${statusTone}`}><button onClick={() => onPreview({ id: request.videoId, source: request.source, spotifyId: request.spotifyId, title: request.title, artist: request.artist, thumbnail: request.thumbnail, previewUrl: request.previewUrl, externalUrl: request.externalUrl })} className="relative h-14 w-[76px] shrink-0 overflow-hidden rounded-xl bg-white/10" aria-label={`${t('preview')}: ${request.title}`}><img src={request.thumbnail} alt="" className="h-full w-full object-cover" /><span className="absolute inset-0 grid place-items-center bg-black/30 text-white"><span className="grid h-6 w-6 place-items-center rounded-full bg-black/35"><Play size={12} fill="currentColor" /></span></span></button><div className="min-w-0 flex-1"><div className="flex min-w-0 items-start gap-2"><div className="min-w-0 flex-1"><h3 className={`live-request__title text-[13px] font-bold leading-[1.2] ${handled ? 'line-through decoration-white/60' : ''}`} style={{ display: '-webkit-box', WebkitBoxOrient: 'vertical', WebkitLineClamp: 2, overflow: 'hidden' }}>{request.title}</h3><p className="mt-0.5 truncate text-[11px] leading-4 text-white/50">{request.artist}</p></div><button onClick={() => onLike(request.id)} disabled={liked || request.status !== 'pending'} className={`flex shrink-0 items-center gap-1 rounded-xl px-2 py-1 text-[11px] font-bold transition ${liked ? 'bg-neon text-ink' : 'bg-white/10 text-white/65 hover:bg-neon/15 hover:text-neon'}`}><ThumbsUp size={12} fill={liked ? 'currentColor' : 'none'} /> {request.likes}</button></div><div className="mt-1 space-y-0.5 text-[10px] leading-4 text-white/40"><p className="flex flex-wrap gap-x-1"><span className="shrink-0 font-semibold text-turquoise">{t('requestedBy')}:</span><span className="break-words">{request.requester || '—'}</span></p><p className="flex flex-wrap gap-x-1"><span className="shrink-0 font-semibold text-white/65">{t('dedicatedTo')}:</span><span className="break-words">{request.dedication || '—'}</span></p><p className={`flex flex-wrap gap-x-1 ${statusText}`}><span className="shrink-0 font-semibold">{t('statusLabel')}:</span><span className="break-words">{statusLabel}</span></p></div></div></article> }
function PreviewFrame({ track }) { if (track.source === 'spotify' || String(track.id || '').startsWith('spotify:')) { const spotifyId = track.spotifyId || String(track.id).replace(/^spotify:/, ''); return <iframe title={track.title} src={`https://open.spotify.com/embed/track/${spotifyId}`} className="h-[352px] w-full" allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture" loading="lazy" /> } if ((track.source === 'deezer' || track.source === 'soundcloud') && track.previewUrl) return <audio controls autoPlay src={track.previewUrl} className="w-full" />; if (track.externalUrl) return <div className="p-8 text-center"><a href={track.externalUrl} target="_blank" rel="noreferrer" className="btn-primary">{track.title}</a></div>; const youtubeId = String(track.id || '').replace(/^youtube:/, ''); return <iframe title={track.title} src={track.videoUrl || `https://www.youtube-nocookie.com/embed/${youtubeId}?autoplay=1&rel=0`} className="aspect-video w-full" allow="autoplay; encrypted-media" allowFullScreen /> }
function QrPlaceholder() { const cells = Array.from({ length: 81 }, (_, index) => { const row = Math.floor(index / 9); const col = index % 9; const finder = (row < 3 && col < 3) || (row < 3 && col > 5) || (row > 5 && col < 3); return <span key={index} className={(finder ? (row === 1 && col === 1) || (row === 1 && col === 7) || (row === 7 && col === 1) : (index * 7 + row * 3) % 5 < 2) ? 'bg-ink' : 'bg-transparent'} /> }); return <div className="grid h-28 w-28 shrink-0 grid-cols-9 gap-1 rounded-xl bg-white p-2">{cells}</div> }

export default function AttendeeApp({ event, onUpdate, onExit }) {
  const { t } = useLanguage(); const [query, setQuery] = useState(''); const [results, setResults] = useState([]); const [searching, setSearching] = useState(false); const [selected, setSelected] = useState(null); const [requestSong, setRequestSong] = useState(null); const [form, setForm] = useState({ requester: '', dedication: '' }); const [notice, setNotice] = useState(''); const [likedIds, setLikedIds] = useState(() => getLikedIds(event.id))
  const sortedRequests = useMemo(() => [...(event.requests || [])].sort((a, b) => b.likes - a.likes), [event.requests])
  async function handleSearch(e) { e.preventDefault(); if (!query.trim()) return; setSearching(true); try { setResults(await searchTracks(query)) } catch { setNotice('No se pudo buscar ahora. Revisa tu conexión e inténtalo de nuevo.') } finally { setSearching(false) } }
  async function submitRequest(e) { e.preventDefault(); if (!form.requester.trim() || !requestSong) return; try { const request = supabaseEnabled && !event.localOnly ? await addSongRequest(event.id, requestSong, form) : { id: `request-${Date.now()}`, title: requestSong.title, artist: requestSong.artist, videoId: requestSong.source === 'youtube' ? requestSong.id : `${requestSong.source}:${requestSong.id}`, source: requestSong.source || 'youtube', thumbnail: requestSong.thumbnail, previewUrl: requestSong.previewUrl, externalUrl: requestSong.externalUrl, requester: form.requester.trim(), dedication: form.dedication.trim(), likes: 0, status: 'pending' }; onUpdate({ ...event, requests: [...(event.requests || []), request] }); setRequestSong(null); setForm({ requester: '', dedication: '' }); setQuery(''); setResults([]); setNotice(t('sent')); setTimeout(() => setNotice(''), 3500) } catch { setNotice('No se pudo enviar el pedido. Inténtalo otra vez.') } }
  async function likeRequest(id) { if (likedIds.includes(id)) return; try { if (supabaseEnabled && !event.localOnly) await likeSongRequest(id); const nextLiked = [...likedIds, id]; const requests = event.requests.map((request) => request.id === id ? { ...request, likes: request.likes + 1 } : request); setLikedIds(nextLiked); saveLikedIds(event.id, nextLiked); onUpdate({ ...event, requests }) } catch { setNotice('No se pudo registrar el like. Inténtalo de nuevo.') } }
  return <AppShell onHome={onExit} right={<div className="flex items-center gap-3"><span className="hidden text-right sm:block"><span className="block text-[10px] uppercase tracking-widest text-white/35">{t('eventCode')}</span><strong className="text-sm">{event.name}</strong></span><span className="rounded-xl border border-neon/20 bg-neon/10 px-3 py-2 text-sm font-bold tracking-widest text-neon">{event.code}</span></div>}><PageContainer><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">{t('attendee')} · {event.code}</p><h1 className="mt-2 font-display text-4xl font-bold tracking-tight sm:text-5xl">{t('attendeeAction')}.</h1><p className="mt-3 max-w-xl text-sm text-white/50">{t('requestHint')} {event.djName}.</p></div><div className="flex items-center gap-2 rounded-full border border-turquoise/25 bg-turquoise/10 px-3 py-2 text-xs font-semibold text-turquoise"><span className="h-2 w-2 animate-pulse rounded-full bg-turquoise" /> {t('active')}</div></div>{notice && <div className="mb-6 flex items-center gap-2 rounded-2xl border border-neon/20 bg-neon/10 px-4 py-3 text-sm text-neon"><Check size={18} /> {notice}</div>}<section className="glass rounded-[2rem] p-5 sm:p-7"><div className="mb-5 flex items-center justify-between"><div><p className="eyebrow">{t('findRhythm')}</p><h2 className="section-title mt-2">{t('attendeeSearch')}</h2></div><Music2 className="text-white/20" /></div><form onSubmit={handleSearch} className="flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search size={19} className="absolute left-4 top-1/2 -translate-y-1/2 text-white/35" /><input value={query} onChange={(e) => setQuery(e.target.value)} className="input-dark pl-11" placeholder={t('searchPlaceholder')} /></div><button className="btn-primary sm:min-w-36" disabled={searching || !query.trim()}>{searching ? <Loader2 size={18} className="animate-spin" /> : <Search size={18} />} {searching ? t('searching') : t('search')}</button></form>{results.length > 0 && <div className="mt-4 space-y-2">{results.map((track) => <SearchResult key={`${track.id}-${track.title}`} track={track} t={t} onPreview={setSelected} onRequest={setRequestSong} />)}</div>}{!results.length && <div className="mt-5 rounded-2xl border border-dashed border-white/10 px-5 py-8 text-center text-sm text-white/35"><Zap size={19} className="mx-auto mb-2 text-neon/70" />{t('writeSong')}</div>}</section><div className="mx-auto mt-8 grid min-w-0 max-w-5xl gap-8 overflow-hidden xl:grid-cols-[minmax(0,1fr)_340px]"><section className="min-w-0"><div className="mb-4 flex items-end justify-between"><div><p className="eyebrow">{t('partyQueue')}</p><h2 className="section-title mt-2">{t('liveRequests')}</h2></div><span className="rounded-full bg-white/10 px-3 py-1.5 text-xs text-white/50">{sortedRequests.length} {t('requestCount')}</span></div><div className="space-y-3">{sortedRequests.map((request) => <RequestCard key={request.id} request={request} liked={likedIds.includes(request.id)} onLike={likeRequest} onPreview={setSelected} t={t} />)}{!sortedRequests.length && <div className="glass rounded-2xl px-5 py-10 text-center text-sm text-white/40">{t('noRequests')}</div>}</div></section><aside className="mx-auto min-w-0 w-full max-w-md overflow-hidden xl:mx-0"><div className="overflow-hidden rounded-[2rem] border border-violet/20 bg-gradient-to-br from-violet/25 via-white/[.04] to-neon/10 p-5 sm:p-6"><div className="mb-5 flex items-center gap-2 text-turquoise"><MessageCircleHeart size={20} /><span className="font-bold">{t('supportDj')}</span></div><p className="text-sm leading-6 text-white/65">{event.thankYou || t('thanksDefault')}</p><div className="mt-5 flex min-w-0 max-w-full flex-col items-center gap-4 overflow-hidden rounded-2xl bg-white p-4 text-center text-ink sm:flex-row sm:text-left">{event.qrImage ? <img src={event.qrImage} alt={t('qrImageLabel')} className="h-28 w-28 shrink-0 rounded-xl object-contain" /> : <QrPlaceholder />}<div className="min-w-0 max-w-full"><p className="text-xs font-bold uppercase tracking-widest text-ink/50">{t('yape')}</p><p className="mt-1 text-lg font-extrabold">{event.yapeNumber || t('defaultYape')}</p><p className="mt-1 text-xs text-ink/60">{t('scanTip')}</p></div></div><div className="mt-5 flex items-center gap-2 text-xs text-white/50"><Clock3 size={14} /> {t('contracting')}: <a href={`tel:${event.contact || ''}`} className="font-semibold text-neon underline-offset-2 hover:underline">{event.contact || t('defaultContact')}</a></div></div><div className="mt-4 rounded-2xl border border-white/10 bg-white/[.03] p-5"><p className="text-xs font-bold uppercase tracking-widest text-white/35">{t('quickTip')}</p><p className="mt-2 text-sm leading-6 text-white/55">{t('queueTip')}</p></div></aside></div></PageContainer>{requestSong && <Modal title={t('requestSong')} onClose={() => setRequestSong(null)}><div className="mb-5 flex gap-3 rounded-2xl bg-white/[.05] p-3"><img src={requestSong.thumbnail} alt="" className="h-16 w-24 rounded-xl object-cover" /><div className="min-w-0"><p className="truncate font-bold">{requestSong.title}</p><p className="mt-1 truncate text-xs text-white/50">{requestSong.artist}</p></div></div><form onSubmit={submitRequest} className="space-y-4"><label className="block"><span className="mb-2 block text-sm font-semibold text-white/75">{t('yourName')} <span className="text-neon">*</span></span><input required value={form.requester} onChange={(e) => setForm({ ...form, requester: e.target.value })} className="input-dark" placeholder={t('namePlaceholder')} /></label><label className="block"><span className="mb-2 block text-sm font-semibold text-white/75">{t('dedication')} <span className="font-normal text-white/35">{t('optional')}</span></span><textarea value={form.dedication} onChange={(e) => setForm({ ...form, dedication: e.target.value })} className="input-dark min-h-24 resize-none" placeholder={t('dedicationPlaceholder')} maxLength={100} /></label><button className="btn-primary w-full"><Send size={17} /> {t('sendRequest')}</button></form></Modal>}{selected && <Modal title={t('preview')} onClose={() => setSelected(null)}><div className="overflow-hidden rounded-2xl bg-black"><PreviewFrame track={selected} /></div><h3 className="mt-4 font-bold">{selected.title}</h3><p className="mt-1 text-sm text-white/50">{selected.artist}</p></Modal>}</AppShell>
}
