import { useState } from 'react'
import { ArrowLeft, ArrowRight, Eye, EyeOff, Headphones, KeyRound, Mail, ShieldCheck } from 'lucide-react'
import { Brand, PageContainer } from './Brand'
import LanguagePicker from './LanguagePicker'
import { signInDj, startDjTrial, supabaseEnabled } from '../lib/supabase'
import { useLanguage } from '../lib/i18n'

export default function DjLogin({ onLogin, onBack }) {
  const { t } = useLanguage(); const [email, setEmail] = useState(''); const [code, setCode] = useState(''); const [show, setShow] = useState(false); const [error, setError] = useState(''); const [planExpired, setPlanExpired] = useState(false); const [showTrial, setShowTrial] = useState(false); const [trialName, setTrialName] = useState(''); const [trialEmail, setTrialEmail] = useState(''); const [trialCode, setTrialCode] = useState(''); const [trialError, setTrialError] = useState('')
  async function requestTrial(e) {
    e.preventDefault(); setTrialError('')
    try { if (!supabaseEnabled) throw new Error('SUPABASE_REQUIRED'); const result = await startDjTrial(trialEmail, trialName); setTrialCode(result.generated_code); setEmail(trialEmail); setCode(result.generated_code) }
    catch (err) { setTrialError(err?.message === 'SUPABASE_REQUIRED' ? t('supabaseRequired') : t('trialUnavailable')) }
  }

  async function submit(e) {
    e.preventDefault(); setError(''); setPlanExpired(false)
    try { if (!supabaseEnabled) throw new Error('SUPABASE_REQUIRED'); onLogin(await signInDj(email, code)) }
    catch (err) { const message = String(err?.message || '').toLowerCase(); const expired = message.includes('plan expired') || message.includes('plan venc'); setPlanExpired(expired); setError(err?.message === 'SUPABASE_REQUIRED' ? t('supabaseRequired') : expired ? t('planExpired') : t('loginError')) }
  }
  return <div className="party-page min-h-screen bg-ink bg-grid"><PageContainer className="flex min-h-screen flex-col"><div className="flex items-center justify-between"><Brand /><div className="flex items-center gap-2"><LanguagePicker /><button onClick={onBack} className="btn-secondary px-3 py-2 text-sm"><ArrowLeft size={16} /> {t('back')}</button></div></div><div className="flex flex-1 items-center justify-center py-14"><div className="w-full max-w-md"><div className="mb-8 text-center"><div className="mx-auto mb-5 grid h-16 w-16 place-items-center rounded-3xl bg-violet/20 text-violet-200"><Headphones size={30} /></div><p className="eyebrow text-violet-200">{t('djAccess')}</p><h1 className="mt-3 font-display text-4xl font-bold tracking-tight sm:text-5xl">{t('djHeading')}</h1><p className="mt-4 text-sm leading-6 text-white/55">{t('djDescription')}</p></div><form onSubmit={submit} className="glass rounded-[2rem] p-5 sm:p-7"><label className="mb-2 block text-sm font-semibold text-white/75">{t('email')}</label><div className="relative"><Mail size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-white/35" /><input autoFocus required type="email" value={email} onChange={(e) => { setEmail(e.target.value); setError(''); setPlanExpired(false) }} className="input-dark pl-11" placeholder={t('emailPlaceholder')} autoComplete="username" /></div><label className="mb-2 mt-4 block text-sm font-semibold text-white/75">{t('accessCode')}</label><div className="relative"><KeyRound size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-white/35" /><input required type={show ? 'text' : 'password'} value={code} onChange={(e) => { setCode(e.target.value); setError(''); setPlanExpired(false) }} className="input-dark pl-11 pr-12" placeholder="Código personal" autoComplete="one-time-code" /><button type="button" onClick={() => setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 rounded-lg p-1.5 text-white/35 hover:text-white" aria-label={show ? 'Ocultar código' : 'Mostrar código'}>{show ? <EyeOff size={18} /> : <Eye size={18} />}</button></div>{error && <div className="mt-3 rounded-xl border border-red-400/20 bg-red-400/10 px-3 py-2 text-sm text-red-200"><p>{error}</p>{planExpired && <a href="mailto:djgianfrancoromerodechosica@gmail.com?subject=Solicitud%20de%20plan%20DJ" className="mt-2 inline-block font-bold text-turquoise underline underline-offset-2">Solicitar un plan</a>}</div>}<button className="btn-primary mt-5 w-full">{t('enter')} <ArrowRight size={18} /></button><div className="mt-5 flex items-center gap-2 text-xs text-white/35"><ShieldCheck size={14} className="text-violet-300" /> {t('codeHint')}</div></form><button type="button" onClick={() => { setShowTrial(true); setTrialError(''); setTrialCode('') }} className="mt-4 w-full rounded-xl border border-turquoise/30 bg-turquoise/10 px-4 py-3 text-sm font-bold text-turquoise hover:bg-turquoise/15">{t('tryDemo')}</button></div></div>{showTrial && <div className="fixed inset-0 z-50 grid place-items-center bg-black/75 p-4 backdrop-blur-sm" onMouseDown={() => setShowTrial(false)}><div className="glass w-full max-w-md rounded-[2rem] p-6" onMouseDown={(e) => e.stopPropagation()}><h2 className="font-display text-2xl font-bold">{t('demoTitle')}</h2><p className="mt-2 text-sm leading-6 text-white/55">{t('demoHintOneDay')}</p>{trialCode ? <div className="mt-5 rounded-2xl border border-turquoise/30 bg-turquoise/10 p-4 text-center"><p className="text-xs text-white/55">{t('demoCodeLabel')}</p><p className="mt-2 font-mono text-3xl font-bold tracking-[.18em] text-turquoise">{trialCode}</p><p className="mt-3 text-xs text-white/55">{t('demoCodeExpires')}</p><button type="button" onClick={() => setShowTrial(false)} className="btn-primary mt-4 w-full">{t('useDemoCode')}</button></div> : <form onSubmit={requestTrial} className="mt-5 space-y-3"><input required value={trialName} onChange={(e) => setTrialName(e.target.value)} className="input-dark" placeholder={t('demoNamePlaceholder')} /><input required type="email" value={trialEmail} onChange={(e) => setTrialEmail(e.target.value)} className="input-dark" placeholder={t('emailPlaceholder')} />{trialError && <p className="rounded-xl border border-red-400/20 bg-red-400/10 px-3 py-2 text-sm text-red-200">{trialError}</p>}<button className="btn-primary w-full">{t('generateDemoCode')}</button></form>}</div></div>}</PageContainer></div>
}
